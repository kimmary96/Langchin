# 어디가 아프니? 2.0 — LangGraph 기반 헬스케어 AI Agent

---

## 1. 서비스 기획

### 서비스 개요

| 항목 | 내용 |
|------|------|
| **서비스명** | 어디가 아프니? (엄마품) |
| **한 줄 정의** | 내 병력을 다 알고 있는 엄마 같은 친구가 아플 때 곁에서 위로해주고 건강을 함께 관리해주는 AI 헬스케어 챗봇 |
| **핵심 가치** | 아플 때 혼자가 아닌 느낌 — 심리적 위로 + 실질적 건강 정보 제공 |

### 타겟 사용자

- **주요 타겟**: 만성 질환이나 반복 증상을 가진 30~50대 여성

- **문제 상황**:

| # | 문제 상황 |
|---|-----------|
| 1 | 아프다고 말해도 공감받지 못하는 서운함 |
| 2 | 반복 증상을 주변에 털어놓기 미안함 |
| 3 | 내 병력을 모르는 챗봇에게 매번 처음부터 설명해야 하는 피로감 |
| 4 | 언제 어떻게 아팠는지 기록이 없어 병원 설명이 어려움 |

### 핵심 기능

| 기능 | 설명 |
|------|------|
| 엄마 페르소나 챗봇 | LangGraph 기반 Plan → Execute → Refine 3단계 그래프로 공감 + 정확성을 갖춘 답변 생성 |
| 병력 등록 및 관리 | 사이드바에서 직접 등록 + 대화 중 자동 추가 (질환명·날짜·메모 저장) |
| 건강일기 캘린더 | 컨디션·증상·배변·수면·운동·병원·메모 7개 카테고리 기록 |
| 약봉투/상처 사진 분석 | OpenAI Vision API(GPT-4o)로 약 이름·효능·용법 또는 상처 상태 분석 |
| 증상 기반 병원 추천 | 60개+ 증상 키워드 → 진료과 매핑 후 네이버 지도 연동 |
| 오늘의 건강 리포트 | 하루 대화 요약을 자동 생성하여 건강 흐름 파악 가능 |

### 한계점 및 개선 방향

**한계점**

| 항목 | 설명 |
|------|------|
| 의학적 진단 불가 | 법적 한계로 인해 진단·처방 기능 제공 불가 |
| 임상 검증 미실시 | 실제 환자 대상 임상 검증을 거치지 않음 |
| 단일 사용자 | 로그인/인증 없이 1인 사용 전제 |
| 복약 알림 미구현 | 알림 UI는 존재하나 실제 백그라운드 발송 미구현 |

**개선 방향**

| 시기 | 개선 내용 |
|------|-----------|
| 단기 | 응답 스트리밍, RAG 기반 의학 정보 정확도 향상 |
| 중기 | 증상 위험도 자동 분류, 월간 건강 트렌드 분석 |
| 장기 | 다중 에이전트, 가족 공유 기능, FastAPI 전환 |

---

## 2. 기술 구조

### 기술 스택

| 분류 | 기술 | 설명 |
|------|------|------|
| AI Agent | `langgraph` | Plan → Execute → Refine 3단계 그래프 구조 |
| LLM | `langchain-openai` (GPT-4o-mini) | 대화 생성·계획·검토에 사용 |
| 웹 검색 | `tavily-python` | 의학 정보·약 정보 실시간 검색 |
| Vision | `openai` (GPT-4o) | 약봉투·상처 사진 분석 |
| UI | `streamlit` | 모바일 퍼스트 챗봇 인터페이스 |
| 프롬프트 관리 | `pyyaml` | 시스템 프롬프트 YAML 구조화 |
| 데이터 저장 | JSON 파일 (로컬) | 병력·건강일기·대화 이력 저장 |

### 프로젝트 구조

```
Langchin/
├── .streamlit/
│   └── config.toml                # Streamlit 테마·서버 설정
├── components/
│   ├── __init__.py                # 패키지 초기화
│   ├── sidebar.py                 # 병력 등록·복약 알림 사이드바 UI
│   ├── alarm_ui.py                # 복약 알림 UI 컴포넌트
│   └── calendar_view.py           # 건강일기 캘린더 다이얼로그
├── data/                          # 데이터 저장 디렉토리 (gitignore)
│   ├── medical_history.json       # 병력 기록
│   ├── health_diary.json          # 건강일기 데이터
│   └── chat_YYYY-MM-DD.json       # 일별 대화 이력
├── app.py                         # Streamlit 메인 애플리케이션
├── chatbot.py                     # MomChatbot 클래스 · 증상→진료과 매핑
├── data_manager.py                # JSON 파일 CRUD 관리
├── prompts.py                     # 시스템 프롬프트 생성
├── vision.py                      # OpenAI Vision API 이미지 분석
├── requirements.txt               # Python 의존성 목록
├── LICENSE                        # MIT 라이선스
└── README.md                      # 프로젝트 문서
```

### LangGraph 구조 설명

본 서비스는 **Plan → Execute → Refine** 3단계 그래프 구조로 동작합니다.

**1단계: Plan 노드 (계획) — temperature: 0.3**

사용자 메시지와 등록된 병력을 분석하여 답변 전략을 수립합니다. 이 단계에서 웹 검색 필요 여부(`search_needed`)를 판단하며, 약 이름·특정 질병명·최신 의학 정보가 포함된 질문은 `search_needed=True`를 강제합니다. 낮은 temperature(0.3)로 정확하고 일관된 계획을 세웁니다.

**2단계: Execute 노드 (실행) — temperature: 0.8**

Plan 단계의 전략에 따라 엄마 페르소나로 답변을 생성합니다. `search_needed=True`인 경우 Tavily 웹 검색을 수행한 후 결과를 반영하여 답변합니다. 재시도 시에는 Refine 노드의 피드백을 반영하여 개선된 답변을 생성합니다. 높은 temperature(0.8)로 자연스럽고 따뜻한 대화 톤을 구현합니다.

**3단계: Refine 노드 (검토 + Cycle) — temperature: 0.1**

생성된 답변의 품질을 공감·정확성·안전성 기준으로 평가합니다. 평가 점수가 7점 미만이면 Execute 노드로 되돌아가는 **Cycle**을 구현하여 답변 품질을 보장합니다. 최대 재시도 1회(`MAX_RETRIES=2`)로 무한 루프를 방지하며, 매우 낮은 temperature(0.1)로 엄격한 평가를 수행합니다.

**조건부 분기:**

- `after_plan`: `search_needed=False` → `execute_direct` (Refine 없이 바로 종료)
- `after_plan`: `search_needed=True` → `execute` → `refine` → `should_retry`
- 이를 통해 **일반 대화는 LLM 2회 호출**, **검색 대화는 최대 5회 호출**로 효율화

### LangGraph 시각화

> graph_visualization.png 또는 graph_visualization.md 파일 첨부

![graph](graph_visualization.png)

---

## 3. 회고

### 1. 프롬프트가 여러 곳에 있으면 LLM이 혼란스러워한다

페르소나(말투, 성격)를 `prompts.py`, `prompts.yaml`, `nodes.py` 세 군데에서 각각 정의했더니 LLM이 어느 쪽을 따라야 할지 몰라 말투가 뒤섞이는 문제가 생겼다. 한 번은 반말로, 다음 답변은 존댓말로 나오는 식이었다. 해결 방법은 단순했다. 페르소나는 `prompts.py` 한 곳에서만 정의하고, `prompts.yaml`은 "무엇을 할지"(작업 지시)만 담도록 역할을 명확히 분리했다. LLM도 사람처럼 여러 곳에서 지시가 충돌하면 혼란스럽다는 걸 직접 경험으로 배웠다.

---

### 2. LLM을 믿으면 안 되는 순간 — 의학 정보 할루시네이션

사용자가 특정 약 이름을 물어봤을 때, LLM이 웹 검색 없이 자체 지식만으로 틀린 정보를 자신 있게 답하는 문제가 발생했다. 헬스케어 서비스에서 잘못된 의학 정보는 단순한 버그가 아니라 신뢰의 문제다. 해결을 위해 약 이름이나 특정 질병명이 등장하면 LLM 판단과 무관하게 무조건 `search_needed=True`로 강제 처리하는 로직을 plan 노드에 추가했다. LLM이 알고 있다고 생각하는 것과 실제로 정확한 것은 다를 수 있고, 특히 의학 도메인에서는 항상 외부 검색을 통해 검증해야 한다는 교훈을 얻었다.

---

### 3. 모든 대화에 LangGraph를 태우면 느려진다

처음 설계에서는 "안녕", "피곤해" 같은 단순한 대화도 Plan → Execute → Refine 전체 3단계를 거쳤다. 결과적으로 응답마다 LLM을 3~5회 호출하게 되어 속도가 너무 느렸다. `after_plan` 조건부 엣지를 추가해서 `search_needed=False`이면 Refine 없이 바로 종료하는 `execute_direct` 경로를 만들었고, 일반 대화의 LLM 호출 횟수를 3회에서 2회로 줄였다. 그래프 구조는 "항상 전부 실행"이 아니라 상황에 따라 분기하도록 설계해야 효율적이라는 걸 배웠다.

---

### 4. 대화가 길어지면 LLM은 앞을 잊는다 — 중간 망각 현상

QA 하면서 LLM이 바로 앞에서 나눈 병력이나 증상을 무시하고 처음 만난 것처럼 대화하는 현상이 발견했다. 전체 히스토리를 통째로 넣으면 토큰이 폭발적으로 늘어나고 중간 내용을 놓치는 "중간 망각" 문제가 생기는 구조적 한계였다. 두 가지 방법을 조합해서 해결했다. 먼저 최근 6개 대화만 넘기는 방식(방법 2)으로 즉시 안정화하고, 6개마다 자동으로 핵심 내용을 요약해서 누적하는 요약 메모리(방법 1)를 추가했다. 이를 통해 토큰 소모를 기존 대비 약 97% 절감하면서도 맥락 유지에 성공했다. 맥락 관리는 LLM 성능의 문제가 아니라 설계의 문제라는 것을 깨달았다.

